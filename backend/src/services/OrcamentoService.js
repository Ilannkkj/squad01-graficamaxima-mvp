class OrcamentoService {
    calcularOrcamento(dados) {
        let custoTotal = 0;

        custoTotaL += Number(dados.custoPapel || 0);
    }
}

// Resumo Financeiro

const custoTotal = "";
const revenda = "";
const vendaComum = "";
const venda = "";
const valorUnitario = "";